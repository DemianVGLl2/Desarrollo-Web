<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game of thrones</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Alex+Brush&family=Bai+Jamjuree&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="./css/index.css">
    <script type="text/javascript" src="../server.js"></script>
    <script type="text/javascript">
      function nextCharacter() {
        window.location.href = '/nextCharacter';
      }
      function previousCharacter() {
        window.location.href = '/previousCharacter';
      }
      function searchByName() {
        //var name = document.getElementBy...('').value;
        window.location.href = '/searchCharacter?name=' + name;
    }
    </script>
</head>
<body>
    <div class="card-group">
        <div class="card">
          <img src="<%= image%>" class="card-img-top" alt="character_mug">
          <div class="card-body">
            <h5 class="card-title txt">Character ID: <%= id%> </h5>
            <h4><p class="card-text txt"><small class="text-body-secondary"><%= first_name%> <%= last_name%></small></p></h4>
          </div>
        </div>
        <div class="card" style="width: 18rem;">
              <h5 class="card-title txt">General info</h5>
              <ul class="list-group list-group-flush">
              <li class="list-group-item txt">Born <%= born_date%></li>
              <% if (dead_date != "") { %>
              <li class="list-group-item txt">Died <%= dead_date%></li>
              <% } %>
              <li class="list-group-item txt">Titles: <% var cont=0; titles.forEach((title, index) => { %>
                <%= title %><% if (index < titles.length - 1) { %>, <% } %>
              <% }) %></li>
              <li class="list-group-item txt">Allegiances: <% var cont=0; alliances.forEach((alliance, index) => { %>
                <%= alliance %><% if (index < alliances.length - 1) { %>, <% } %>
              <% }) %></li>
              <h5 class="card-title txt"><%= family%></h5>
            </ul>
          </div>
      </div>
      <div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleRide" data-bs-slide="prev"
        onclick="previousCharacter()">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleRide" data-bs-slide="next"
        onclick="nextCharacter()">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>      
      </div>
      <form action="/searchCharacter" method="get">
        <label for="idchange">Character Name:</label>
        <input type="text" name="name" id="name" step="any" placeholder="# of character for info to be displayed"/>
        <br/>
        <input type="submit" value="Submit" onclick="searchByName()"/> 
      </form>
</body>
</html>